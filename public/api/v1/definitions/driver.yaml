title: Driver
type: object
required:
  - name
  - photoUrls
properties:
  id:
    type: integer
    format: int64
  user_id:
    type: integer
    example: int64
  created_at:
    type: timestamp
    example: canonical
  updated_at:
    type: timestamp
    example: canonical
  #photoUrls:
    #type: array
    #xml:
    #  name: photoUrl
    #  wrapped: true
    #items:
    #  type: string
  status:
    type: string
    description: pet status in the store
    enum:
      - available
      - pending
      - sold
xml:
  name: Driver